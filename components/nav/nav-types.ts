export type FavoriteEntry = {
  boardId: string;
  boardTitle: string;
  workspaceId: string;
  workspaceName: string;
};

export type RecentEntry = {
  boardId: string;
  boardTitle: string;
  workspaceId: string;
  workspaceName: string;
};
