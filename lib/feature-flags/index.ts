export type FlagName = "shared_workspace_cache_v2" | "subboards_enabled" | "virtualized_board" | "auth_broadcast_v1" | "lazy_card_history";

// TODO(1b-followup): gate the sub-board affordance on useWorkspaceFlag("subboards_enabled").
